#!/bin/bash

if [ -z "$DEV_SDCARD" ]
then
	echo ACME_SDCARD not set
	exit -2
fi

size=$(df -k $DEV_SDCARD | tail -1 | awk '{print $2}')

if [ "$size" -gt "170000000" ]
then
        echo $DEV_SDCARD "has suspiscious size of" $size "bytes"
	echo "refusing to process"
        exit -1
fi

